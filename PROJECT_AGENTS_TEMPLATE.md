# `<project-name>` Agent 规则模板

将本文件内容复制到新子项目的 `AGENTS.md`，填入实际资讯后删除本说明。不要保留猜测、占位命令或不适用的章节。

## 项目定位

- 目的：`<这个项目解决什么问题>`
- 技术栈：`<语言、框架、主要版本>`
- 版本控制根目录：`<通常为当前目录>`
- 主要入口：`<路径>`

## 目录地图

- `<path>`：`<职责>`
- `<path>`：`<职责>`
- 生成档案：`<不可手动编辑的路径>`

## 常用命令

- 安装：`<command>`
- 开发：`<command>`
- 测试：`<command>`
- lint：`<command>`
- typecheck：`<command>`
- 构建：`<command>`

只保留已在本项目验证过的命令。

## 工程规范

- `<命名、格式、错误处理、相容性或架构约束>`
- `<新增依赖前的要求>`
- `<资料库、API、迁移或安全规则>`

## Agent 使用

- 简单任务由主 agent 完成。
- 唯读探索：`<内建 explorer、根目录 workspace_explorer 或项目专属 agent>`
- 一般实作：`<根目录 project_worker 或项目专属 agent>`
- 独立审查：`<根目录 project_reviewer 或项目专属 agent>`
- 只有主 agent 可以委派；subagent 不得再产生 subagent。
- `<列出这个项目何时必须或不得委派>`

如需项目专属 agent，请放在 `.codex/agents/<agent-name>.toml`，并让它只承担一个清楚角色。不要复制一个高成本设定到所有工作类型。

## 完成条件

- `<必须通过的测试或检查>`
- `<文件、迁移、变更日志或验收要求>`
- 不触碰本项目以外的目录。

## 禁止事项

- `<不可修改的档案或目录>`
- `<不可执行的破坏性命令>`
- `<机密、生产环境或资料处理限制>`
