# 多项目工作区规则

## 工作区性质

- `/Users/orderly_ray/Projects` 是独立开发项目的容器，不是单一产品、套件或 monorepo。
- 每个第一层子目录默认是独立项目；不得推断项目之间共享程式码、依赖、Git 历史、命令、环境变量、架构或发布流程。
- 根目录只放工作区治理与模板。除非任务明确针对根目录规则，否则不要在根目录新增语言工具链、应用程式码或共用依赖。
- 这个工作区与任何 `Orderly` 目录或项目完全无关。除非使用者明确将它列为当前目标，否则不要读取、搜寻、复制、引用或修改其内容。

## 确定项目边界

开始工作前：

1. 先确定唯一的目标子项目。
2. 进入目标项目后，读取其 `AGENTS.md`、README、版本控制边界及构建清单。
3. 以距离工作档案最近的 `AGENTS.md` 为准；子项目规则可覆盖本文件的一般规则。
4. 从 manifest、lockfile、工具配置与现有脚本判断语言和命令，不要从其他子项目类推。
5. 已知目标项目时，将搜寻、测试和 Git 命令限制在该项目内。

若请求可能指向多个项目，而且错误选择会造成修改，先向使用者确认目标。不得为了寻找线索而扫描其他不相关项目。

## Agent 路由

主 agent 负责范围、决策、整合和最终交付。简单、连续、可由单一 agent 完成的任务留在主 agent。

只有在使用者明确要求 agent/subagent、或任务包含真正独立且有界的并行工作时，才进行委派：

- `workspace_explorer`：唯读探索目标项目、辨识技术栈、命令、影响范围与证据。
- `project_worker`：在单一目标项目内完成范围明确的一般实作与验证。
- `project_reviewer`：对已完成的变更做独立、唯读的正确性、安全性与测试审查。

委派限制：

- 一次最多使用 3 个 subagent；能用更少就不要增加。
- 只有主 agent 可以委派。subagent 不得再产生 subagent。
- 不要让多个写入型 agent 同时修改相同档案或重叠范围。
- 每个委派任务必须写明目标项目、可触碰范围、预期产出和验证方式。
- 除非使用者明确要求跨项目工作，否则一个任务只处理一个子项目。
- subagent 应回传简洁结论与档案证据，不要把完整日志或大段原始输出带回主上下文。

若子项目定义了专属 agent，优先使用子项目的定义；根目录 agent 只是跨语言的安全后备角色。

## 上下文与额度控制

- 先用 `rg`、manifest 和针对性档案读取定位问题，避免无界限地读取整个工作区。
- 不重复读取未变化的档案，不贴出不必要的长日志；保留错误核心与可复现证据即可。
- 长任务开始前先写清楚完成条件。达到条件并通过必要验证后停止，不延伸重构或探索。
- 不因任务困难而自动提升并行度或递回委派；先缩小问题、验证假设，再决定是否需要专属 agent。
- 缺少凭证、外部权限或会实质改变结果的使用者选择时，回报阻碍，不用更多 agent 猜测。

## 修改与验证

- 遵循目标项目现有风格与工具，不在根目录统一语言格式。
- 不修改目标项目以外的档案；发现其他项目有问题时只说明，不顺手修复。
- 执行与变更风险相称的测试、lint、typecheck 或构建。命令必须来自目标项目文件或其文件说明。
- 无法执行验证时，明确说明未执行项目与原因，不宣称已经通过。
- 交付时简述修改档案、行为变化、验证结果，以及仍存在的风险或后续动作。

建立新子项目时，以 `PROJECT_AGENTS_TEMPLATE.md` 为起点，并删除所有不适用或尚未确认的内容；准确的短规则优于推测性的完整规则。

## 健身房换机入口

先读 `GYM-HANDOFF.md` 与 Leader/MIGRATION.md；旧绝对路径按新电脑位置映射。已有模型配置原样保留，不自动启用 disabled 配置。D2 已交付待 Leader 独立验收，不得自行开始 D3。

## thinkV2 迁移候选

- 入口为`handoff/thinkV2/README.md`，产品保持独立checkout，使用项目自己的AGENTS与manifest。
- 编写、审查或重构thinkV2时优先读取所附`handoff/thinkV2/skills/karpathy-guidelines/SKILL.md`（若独立checkout路径不同，按交接指南定位）。
- 原始CP7提交88d3861仍待Leader验收；新机需新建任务并显式重绑角色，不继承旧聊天运行态或授权。
- 交接内部资料已获用户明确公开批准；受限SenseVoice原件只作私有保全，不公开、不自动执行。
